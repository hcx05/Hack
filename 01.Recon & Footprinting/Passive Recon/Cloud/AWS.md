---
S3(Simple Storage Service) : Core services of AWS, like a unlimited cloud disk(cloud bucket).

Bucket : The unit where S3 archives are stored.

	Access Key ID : Like an identification number.
	Secret Access Key : password.

	These two strings are called IAM Credentials.
