
---

`%27` is the URL encoded form of ', + is the URL encoded form of `space`
and `%3D` is the URL encoded form of = .

`SELECT * FROM products WHERE category = '' OR 1=1 --' AND released = 1`
nomatter what's the value of `category` , when condition  `OR 1=1` exists, the entire statement will be true.

`information_chaema.table` is the specific table in `Mysql` that contains all databases' name in the current server.

